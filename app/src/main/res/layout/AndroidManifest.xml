<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="herramienta.KitDeHerramientasDigital">
    <uses-permission android:name="android.permission.CAMERA" />
     <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
          <activity android:name=".LectorQRActivity" android:exported="false" />
          <activity android:name=".SalaTrabajoActivity" />
          <activity android:name=".BlocNotasActivity" />
          <activity android:name=".Carpeta" />
          <activity android:name=".LectorExcel" />
          <activity android:name=".LectorWord" />
          <activity android:name=".LectorPowerpoint" />
    </application>
